# Chef OS Entry Point

The canonical operating system is `knowledge/00-Chef-OS-Manifest.md` in the default branch of `DanieliusIsiunas/cookbook`.

Read that manifest first and follow its task-specific read order. Do not reconstruct canonical recipes from chat memory when GitHub is available.
